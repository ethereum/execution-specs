# AGENTS.md

Read and follow `CLAUDE.md` in full before making changes or reviewing pull
requests. It is the canonical repository guidance shared by Claude and Codex.

Codex discovers the shared skills through `.agents/skills/`. Each Codex skill
points to its canonical instructions in `.claude/commands/` so both agents use
the same workflow.
