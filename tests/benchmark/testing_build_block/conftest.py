"""
Shared fixtures for testing-build-block benchmark scenarios.

The plugin package registers all ``--bloat-*`` CLI options and the
``bloat_*`` fixtures. Spamoor configuration fixtures come from the
sibling spamoor plugin, which is also loaded via the ini file.
"""
