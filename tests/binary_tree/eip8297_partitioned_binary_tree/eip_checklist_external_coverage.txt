general/code_coverage/eels = ethereum.binary_trie / ethereum.state_pbt are exercised directly by the tests/binary_trie/ unit suites (test_trie.py, test_embedding.py, test_state_pbt.py, test_differential_mpt.py, test_fork_parity.py) and end to end by every fill of tests/binary_tree/eip8297_partitioned_binary_tree through the EELS t8n
general/code_coverage/test_coverage = suite logic is exercised end-to-end by filling tests/binary_tree/eip8297_partitioned_binary_tree with the EELS filler, by the tests/binary_trie/ unit suites, and by the ported-EEST sweep (`just fill-binary-tree tests`, reinterpreting the whole tests/ tree through BinaryTree per docs/filling_tests/filling_tests_dev_fork.md)
general/code_coverage/missed_lines = tests/binary_trie/test_fork_parity.py pins binary_tree's *.py files as matching amsterdam's modulo ALLOWED_DELTAS (the state-provider swap and the PreviousHeader re-parenting, both pinned across fork.py and vm/gas.py), fork-name-spelling normalization, and __init__.py's wholesale allowance, so amsterdam's own line coverage plus the differential/embedding unit suites account for every line the fork adds
block_level_constraint = the BinaryTree fork is meant to enforce that the block header's `state_root` equals the tree-computed root -- rejecting a block whose header carries a wrong value rather than merely computing and reporting the correct one. tests/binary_tree/eip8297_partitioned_binary_tree/test_multi_block.py::test_block_with_wrong_state_root_is_rejected records this expectation by injecting a wrong `state_root` via `rlp_modifier`, but that is exactly the condition under which `fill` skips its own block-exception verification, so nothing checks the expectation at fill time, nor does any client, since none consumes BinaryTree fixtures; a `tests/binary_trie/` unit test is where a direct, checked guarantee belongs. This section's boundary/content/fork-transition sub-items are shaped for a numeric-threshold constraint (e.g. a size limit); a root-equality check has none of those axes, so this one test records the constraint's existence, not per-axis variation across boundary values, block content, or fork-transition timing
