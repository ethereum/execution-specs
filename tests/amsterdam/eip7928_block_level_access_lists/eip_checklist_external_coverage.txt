general/code_coverage/eels = Measured with --cov=ethereum.forks.amsterdam while filling this suite with --until=Amsterdam (2,505 cases): block_access_lists.py 234/236 statements and state_tracker.py 170/172. The only BAL-specific miss is the add_code_change replace branch (see missed_lines); the BAL lines of fork.py in state_transition and apply_body are off the fill path, which drives the t8n directly, and run when fixtures are consumed by EELS (just json-loader)
general/code_coverage/test_coverage = Measured with --cov=tests.amsterdam.eip7928_block_level_access_lists: 3,064/3,081 statements covered; the 17 unexecuted statements are all unhandled-enum guard raises that parametrization never reaches
general/code_coverage/second_client = Cross-client coverage tracked separately via the multi-client BAL conformance effort
general/code_coverage/missed_lines = add_code_change's replace branch (block_access_lists.py) needs two system calls at one block access index changing the same account's code, which nothing on mainnet does; its storage, balance and nonce twins are covered. The two state_tracker.py misses are outside the BAL: the transient-storage clear-to-zero pop in set_transient_storage, and the insufficient-balance assertion in move_ether that the CALL value check makes unreachable. Re-verify with uv run fill --until=Amsterdam --cov=ethereum.forks.amsterdam --cov-report=term-missing tests/amsterdam/eip7928_block_level_access_lists
