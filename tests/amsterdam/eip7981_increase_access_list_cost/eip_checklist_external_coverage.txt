general/code_coverage/eels = The EIP-7981 fill exercises every access list surcharge statement of the Amsterdam intrinsic cost calculation, including both gas-used branches, absent and empty lists, repeated entries and keys, all repriced transaction types, contract creation, the recipient and value variants, and sender balance boundaries.
general/code_coverage/test_coverage = Every statement of the test modules in this directory runs during the fill. The only unexecuted lines are the unused branches of the shared recipient fixture.
general/code_coverage/missed_lines = Unexecuted lines in the Amsterdam transaction module are decoding, validation and signature paths outside the access list surcharge; their coverage belongs to the tests for those rules.
