general/code_coverage/eels = Measured by the coverage-enabled fill recipe in the justfile (`--cov=ethereum`, report via `just coverage`), whose `coverage.xml` the `test.yaml` workflow uploads to Codecov; re-verify by opening the report for `src/ethereum/forks/amsterdam/fork.py` (`process_general_purpose_requests`) and `requests.py` after this suite is filled
general/code_coverage/test_coverage = Same recipe with `--cov tests/amsterdam/eip8282_builder_execution_requests`; every module in the directory is collected by the fill, so unreached test code shows up there
general/code_coverage/missed_lines = Read off the Codecov report for this PR's fill; the EIP-8282 execution-layer paths are the two system calls and the two type bytes in `process_general_purpose_requests` and `requests.py`
general/code_coverage/second_client = Cross-client coverage is tracked by the Glamsterdam devnet consume-engine runs, not per EIP
system_contract/test/inputs/invalid/crypto = The execution layer treats the pubkey and signature as opaque bytes; the BLS proof-of-possession is verified by the consensus layer after dequeue (`process_builder_deposit_request` in consensus-specs), so cryptographic invalidity is covered by the consensus-layer spec tests
