general/code_coverage/eels = Measured 2026-09-16 with --cov=ethereum.forks.amsterdam while filling this suite (829 fixtures). These EIP-8038 charge sites have no missed statements at all: calculate_intrinsic_cost (transactions.py) for the repriced intrinsic and access-list costs; sload (vm/instructions/storage.py); balance, extcodesize, extcodecopy and extcodehash (vm/instructions/environment.py); generic_call, callcode, delegatecall, staticcall and generic_create (vm/instructions/system.py), including both account-creation refund branches; resolve_delegated_code_address and set_delegation (vm/eoa_delegation.py), the former in both warmths; and vm/__init__.py (incorporate_child) at 100%. In sstore, call, create, create2 and selfdestruct the only unexecuted lines are the EIP-214 static-context and EIP-3860 init-code-size raises enumerated under missed_lines, none of which EIP-8038 touches. The GasCosts/StateGasCosts tables and the charge_gas/check_gas/charge_state_gas/credit_state_gas_refund/withhold_create_gas/drain_state_gas_reservoir helpers in vm/gas.py are fully executed.
general/code_coverage/test_coverage = Measured 2026-09-16 with --cov=tests.amsterdam.eip8038_state_access_gas_cost_increase --cov-branch: 1789/1792 statements and 69/72 branches covered by 829 passing fixtures. Fourteen of the eighteen modules are at 100% statements and 100% branches.
general/code_coverage/missed_lines = Three test-code statements are unexecuted, all unreachable by construction: the two `raise ValueError(f"unknown invalidity: ...")` fallbacks in test_set_code_auth_gas.py (292, 436), whose parametrizations enumerate only handled values, and the loop body of `_ancestor_gas_costs` in test_exact_balance_no_fallback.py (60), because the immediate ancestor fork always already differs in the selected constants, so the walk never climbs more than one fork; the loop is defensive generality for a future fork that leaves a constant unchanged across a boundary. On the spec side no missed statement belongs to EIP-8038. Outside it: the static-context raises for SSTORE, TSTORE, CREATE, CREATE2, value-CALL and SELFDESTRUCT (EIP-214, unchanged by this EIP and covered by the static-call suites); the MAX_INIT_CODE_SIZE raises in create and create2 (EIP-3860); InvalidContractPrefix and MAX_CODE_SIZE in process_create (EIP-3541, EIP-170); the invalid-r, invalid-s and nonce-at-maximum rejections in eoa_delegation.py (EIP-7702 signature validation); and transaction encoding, decoding, signature recovery and blob-gas accounting. Two are unreachable rather than untested: the StackDepthLimitError in process_call, since the 63/64 rule caps the cheapest possible recursion at depth 491 and reaching 1024 would need 4,480x the EIP-7825 transaction gas cap, and the `gas_left < extra_gas + memory_cost` guard in calculate_message_call_gas, since CALLCODE, DELEGATECALL and STATICCALL all check_gas that exact sum immediately beforehand while CALL passes zero for both. charge_value_transfer_to_non_alive_account and the AddressCollision raise in create_evm are covered by the EIP-2780 and EIP-8037 suites respectively, as recorded in tests/amsterdam/eip2780_reduce_intrinsic_tx_gas/eip_checklist_external_coverage.txt.
gas_refunds_changes/test/cross_functional/calldata_cost = Covered by tests/amsterdam/eip7976_increase_calldata_floor_cost/test_refunds.py::test_gas_refunds_from_data_floor[refund_type_RefundTypes.STORAGE_CLEAR], which runs at Amsterdam and reads REFUND_STORAGE_CLEAR from the fork gas model, so it exercises the EIP-8038 refund value (11,616) against the EIP-7976 calldata floor. Its three arms pin every relationship between the refunded execution gas and the floor: execution-minus-refund above the floor (execution charged), below it (floor charged) and exactly equal to it. The floor mechanics belong to that suite, and duplicating them here would assert the same thing against a second copy of the same derivation.
