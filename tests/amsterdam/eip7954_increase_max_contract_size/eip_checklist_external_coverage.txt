general/code_coverage/eels = EIP-7954 adds only the raised MAX_CODE_SIZE and MAX_INIT_CODE_SIZE constants in forks/amsterdam/vm/interpreter.py; their four comparisons (creation-transaction check in transactions.py, CREATE and CREATE2 checks in vm/instructions/system.py, code deposit check in interpreter.py) are each driven in both directions by this suite
general/code_coverage/test_coverage = Fill the suite with `uv run fill --cov tests/amsterdam/eip7954_increase_max_contract_size`; every case asserts deployed code, contract storage or a transaction rejection, so no case passes vacuously
general/code_coverage/missed_lines = No EIP-7954 lines are excluded; the raised limits are constants compared in four places, all covered at the limit and one byte over it
